<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Stage BTS</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
    <style>
        .nav-link.active {
            background-color: #7433ff;
        }

        .slideshow-container {
            position: relative;
            max-width: 100%;
            height: 400px;
            overflow: hidden;
        }

        .mySlides img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .text {
            position: absolute;
            bottom: 20px;
            left: 20px;
            color: white;
            font-size: 20px;
            background-color: rgba(0, 0, 0, 0.5);
            padding: 10px;
            border-radius: 8px;
        }

        .dot-container {
            position: absolute;
            bottom: 10px;
            width: 100%;
            text-align: center;
        }

        .dot {
            height: 15px;
            width: 15px;
            margin: 0 2px;
            background-color: #bbb;
            border-radius: 50%;
            display: inline-block;
            cursor: pointer;
            transition: background-color 0.6s ease;
        }

        .dot.active {
            background-color: #7433ff;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a href="index.php" class="nav-link {{ pageActive == 'accueil' ? 'active' : '' }}">
                                <img src="icons/home.png" alt="Accueil" width="30" height="24"> Accueil
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="recherche.php" class="nav-link {{ pageActive == 'recherche' ? 'active' : '' }}">Recherche</a>
                        </li>
                        <li class="nav-item">
                            <a href="evenement.php" class="nav-link {{ pageActive == 'evenement' ? 'active' : '' }}">Événements</a>
                        </li>
                        <li class="nav-item">
                            <a href="avis.php" class="nav-link {{ pageActive == 'avis' ? 'active' : '' }}">Avis</a>
                        </li>
                        <li class="nav-item">
                            <a href="programme.php" class="nav-link {{ pageActive == 'programme' ? 'active' : '' }}">Programme</a>
                        </li>
                        <li class="nav-item">
                            <a href="aide.php" class="nav-link {{ pageActive == 'aide' ? 'active' : '' }}">Aide</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <main>
        <!-- Diaporama d'images -->
        <div class="slideshow-container">
            <div class="mySlides">
                <img src="images/restaurant.jpg" alt="Restaurant">
                <div class="text">Découvrez les meilleurs restaurants !</div>
            </div>
            <div class="mySlides">
                <img src="images/hotel.jpg" alt="Hôtel">
                <div class="text">Séjournez dans les hôtels les plus confortables !</div>
            </div>
            <div class="mySlides">
                <img src="images/parc.jpg" alt="Parc">
                <div class="text">Explorez les parcs magnifiques de la France !</div>
            </div>
        </div>

        <!-- Contenu de la page Accueil -->
        <div class="container mt-4">
            <h1>Bienvenue à Votre Guide de Voyage en France</h1>
            <p>Explorez les meilleurs restaurants, hôtels, événements et plus encore avec notre application dédiée à votre séjour en France.</p>
            <p>Ce guide vous propose des informations précieuses sur les destinations incontournables, les événements à venir et des conseils pour rendre votre séjour agréable.</p>
            <!-- Vous pouvez ajouter ici plus de contenu si nécessaire -->
        </div>
    </main>

    <footer>
        <p class="text-center">&copy; Site des meilleures destinations en France</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        let slideIndex = 0;
        showSlides();

        function showSlides() {
            let i;
            let slides = document.getElementsByClassName("mySlides");
            let dots = document.getElementsByClassName("dot");

            // Masquer toutes les diapositives
            for (i = 0; i < slides.length; i++) {
                slides[i].style.display = "none";
            }

            // Réinitialiser l'index si nécessaire
            slideIndex++;
            if (slideIndex > slides.length) {slideIndex = 1}

            // Afficher la diapositive
            slides[slideIndex - 1].style.display = "block";

            // Changer l'image toutes les 3 secondes
            setTimeout(showSlides, 3000);
        }
    </script>
</body>
</html>
